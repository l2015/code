#include<stdio.h>
struct st
{
    char Name[10];
    float Chinese;
};

void Sub(char sub);

int main(){
    struct st 1 =
    struct st 2 = ("2",65);
    struct st 3 = ("2",11);
    struct st 4 = ("2",99);
    Sub(Chinese);
}

void Sub(char sub){
    Arr[i]=st.sub;
    for ( i = 0; i < num; i++)
        {
            if (max < Arr[i])
            {
                max = Arr[i];
            }
            
        }
    
}